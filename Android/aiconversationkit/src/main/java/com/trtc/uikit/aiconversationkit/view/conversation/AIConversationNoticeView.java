package com.trtc.uikit.aiconversationkit.view.conversation;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.FrameLayout;

import androidx.annotation.NonNull;
import androidx.annotation.Nullable;

public class AIConversationNoticeView extends FrameLayout {
    public AIConversationNoticeView(@NonNull Context context) {
        this(context, null);
    }

    public AIConversationNoticeView(@NonNull Context context, @Nullable AttributeSet attrs) {
        super(context, attrs);
    }
}
